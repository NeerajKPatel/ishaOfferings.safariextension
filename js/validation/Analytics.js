/* Show and compute validator analytics */


/*

1. Submit message from message list to log (messages, date, time, user)
2. Show summary of log
	-- Count occurrence for each message
3. Show frequency of each error
4. Show frequency as percent of total

*/

function logResults() {
// Get Results
// Submit messages to DB

}

function percentOfTotal(amount, total) {
	if(total != 0) {
		return amount / total;
	}
}

function showFrequencyOfEachError() {}

function showSummary() {}

